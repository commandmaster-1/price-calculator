export function PlaceholderHint() {
  return (
    <p className="text-xs text-muted-foreground">
      Verwenden Sie die Platzhalter <code>{"{services}"}</code> für die
      ausgewählten Leistungen und <code>{"{price}"}</code> für den Gesamtpreis.
    </p>
  );
}